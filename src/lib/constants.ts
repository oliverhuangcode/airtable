// UI layout constants shared across components

/** Height of each grid row in pixels */
export const ROW_HEIGHT = 32;

/** Default column width in pixels */
export const COL_WIDTH = 180;

/** Number of records per page in infinite scroll */
export const PAGE_SIZE = 10000;

/** Minimum width of the view sidebar in pixels */
export const SIDEBAR_MIN_WIDTH = 280;

/** Maximum width of the view sidebar in pixels */
export const SIDEBAR_MAX_WIDTH = 720;

/** Debounce delay (ms) before auto-saving view config changes */
export const VIEW_CONFIG_DEBOUNCE_MS = 600;
